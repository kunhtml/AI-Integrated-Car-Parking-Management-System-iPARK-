import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Đăng ký hộ | iPARK",
  description: "Đăng ký phương tiện hộ khách hàng tại quầy.",
};

export default function AssistedRegistrationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
